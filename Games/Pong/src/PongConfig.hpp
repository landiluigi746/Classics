#pragma once

#include "Config.hpp"

namespace Classics::Config::Pong
{
    static constexpr float paddleWidth = 20.0f;
    static constexpr float paddleHeight = 128.0f;
    static constexpr float paddleSpeed = 1200.0f;

    static constexpr float roundness = 1.5f;

    static constexpr float ballRadius = 12.5f;
	static constexpr float ballSpeed = 570.0f;

    static constexpr size_t winScore = 12;
}